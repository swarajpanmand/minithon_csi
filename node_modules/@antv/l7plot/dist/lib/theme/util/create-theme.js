"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTheme = createTheme;
exports.createDarkTheme = createDarkTheme;
const tslib_1 = require("tslib");
const util_1 = require("@antv/util");
const create_by_style_sheet_1 = require("./create-by-style-sheet");
const light_1 = require("../style-sheet/light");
const dark_1 = require("../style-sheet/dark");
function createTheme(themeCfg) {
    const { styleSheet: styleSheetCfg = {} } = themeCfg, themeObject = tslib_1.__rest(themeCfg, ["styleSheet"]);
    // ① 创建样式表 (默认创建 light 的样式表)
    const styleSheet = (0, light_1.createLightStyleSheet)(styleSheetCfg);
    // ② 创建主题
    return (0, util_1.deepMix)({}, (0, create_by_style_sheet_1.createThemeByStyleSheet)(styleSheet), themeObject);
}
function createDarkTheme(themeCfg) {
    const { styleSheet: styleSheetCfg = {} } = themeCfg, themeObject = tslib_1.__rest(themeCfg, ["styleSheet"]);
    // ① 创建样式表
    const styleSheet = (0, dark_1.createDarkStyleSheet)(styleSheetCfg);
    // ② 创建主题
    return (0, util_1.deepMix)({}, (0, create_by_style_sheet_1.createThemeByStyleSheet)(styleSheet), themeObject);
}
//# sourceMappingURL=create-theme.js.map