import { deepAssign } from '../../utils';
import { DotLayer } from '../dot-layer';
export class ColumnLayer extends DotLayer {
    constructor() {
        super(...arguments);
        /**
         * 图层类型
         */
        this.type = DotLayer.LayerType.ColumnLayer;
    }
    /**
     * 获取默认配置
     */
    getDefaultOptions() {
        return deepAssign({}, DotLayer.DefaultOptions, {
            shape: 'cylinder',
        });
    }
}
//# sourceMappingURL=index.js.map