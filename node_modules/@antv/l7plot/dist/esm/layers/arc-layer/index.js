import { deepAssign } from '../../utils';
import { LinesLayer } from '../lines-layer';
export class ArcLayer extends LinesLayer {
    constructor() {
        super(...arguments);
        /**
         * 图层类型
         */
        this.type = LinesLayer.LayerType.ArcLayer;
    }
    /**
     * 获取默认配置
     */
    getDefaultOptions() {
        return deepAssign({}, LinesLayer.DefaultOptions, {
            shape: 'arc',
        });
    }
}
//# sourceMappingURL=index.js.map