import { deepAssign } from '../../utils';
import { LinesLayer } from '../lines-layer';
const LAYER_OPTIONS_KEYS = ['color', 'size', 'style', 'state', 'animate'];
export class PathLayer extends LinesLayer {
    constructor() {
        super(...arguments);
        /**
         * 图层类型
         */
        this.type = LinesLayer.LayerType.PathLayer;
    }
    /**
     * 获取默认配置
     */
    getDefaultOptions() {
        return deepAssign({}, LinesLayer.DefaultOptions, {
            shape: 'line',
        });
    }
}
/**
 * 图层配置项 Keys
 */
PathLayer.LayerOptionsKeys = LinesLayer.LayerConfigkeys.concat(LAYER_OPTIONS_KEYS);
//# sourceMappingURL=index.js.map