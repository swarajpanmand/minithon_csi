import { deepAssign } from '../../utils';
import { DotLayer } from '../dot-layer';
const LAYER_OPTIONS_KEYS = ['color', 'size', 'style', 'state', 'animate'];
export class DotDensityLayer extends DotLayer {
    constructor() {
        super(...arguments);
        /**
         * 图层类型
         */
        this.type = DotLayer.LayerType.DotDensity;
        /**
         * 图层是否具有交互属性
         */
        this.interaction = false;
    }
    /**
     * 获取默认配置
     */
    getDefaultOptions() {
        return deepAssign({}, DotLayer.DefaultOptions, {
            shape: 'dot',
            size: 1,
        });
    }
}
/**
 * 图层配置项 Keys
 */
DotDensityLayer.LayerOptionsKeys = DotLayer.LayerConfigkeys.concat(LAYER_OPTIONS_KEYS);
//# sourceMappingURL=index.js.map